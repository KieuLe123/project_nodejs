<% statusHelper = (status, id ) => { 
    let link =  systemConfig.prefixAdmin + '/items/change-status/' + id + '/' + status;
    
     if(status == 'inactive') {
       
%>

        <a href="<%= link  %>" class="my-btn-state rounded-circle btn btn-sm btn-danger"><i class="fas fa-minus"></i></a>

        <% }else{%>

        <a href="<%= link  %>" class="my-btn-state rounded-circle btn btn-sm btn-success"><i class="fas fa-check"></i></a>

        <%  }
}%>